CREATE TABLE [dbo].[ProfileScaleSettingsRules] (
    [Id]   INT           IDENTITY (1, 1) NOT NULL,
    [Name] NVARCHAR (50) NULL,
    CONSTRAINT [PK_ProfileScaleSettingsRules] PRIMARY KEY CLUSTERED ([Id] ASC)
);

